# Cold review — spanweave run 5

**Range.** `0533951..67c7642` on `audit-fixes` (13 commits: seven batch commits
S1–S7, six `plan:` commits).
**Protocol.** §0.2 of `WORKPLAN.md` as at `15cfab5` (`git show 15cfab5:WORKPLAN.md`
— the file is deleted in the tree), plus the `CONTRIBUTING.md` bar and `AGENT.md`
*Self-verification*.
**Method.** One sub-agent per batch, each reading only its own commit; failure
evidence taken in `git worktree` checkouts of the parent, never by mutating the
working tree. Read-only: no tracked file edited, nothing committed, nothing
pushed. The tree ended as it started (`?? patches/` only) and every worktree was
removed.
**Date.** 2026-09-12. CPython 3.12.3 (one reviewer also ran 3.14.6).

---

## 0. Verdict

**No finding blocks the PR.** Nothing in the range produces a traceback on a
supported interpreter, and nothing violates a `CLAUDE.md` invariant. **33 open
threads** follow, each with the sentence to add to `TASKS.md`.

The three gates at HEAD (`67c7642`), run by the orchestrator:

| Gate | Result |
|---|---|
| `make check` | **exit 0** — `2490 passed, 2 skipped`; gates `80 passed`; `spanweave 0.9.1 (graph schema 0.1; UNFROZEN)` |
| `make conformance` | **exit 0** — `580 passed, 2 skipped` |
| `make install-check` | **exit 0** — `install-check green: 33 checks, 4 plants held` |

Range-level hygiene, checked directly:

- `tests/serialized_shape.json` **unchanged** across the whole range.
- Only three files under `spanweave/` move, all in `5e8a40f`:
  `adapters/openinference.py`, `adapters/otel_genai.py`, `seam.py` (+71/−19).
  Every other commit is documents, tests or packaging.
- No `hash()`, clock, randomness, network import, `pickle`, `yaml.load`, `eval`
  or `exec` under `spanweave/`. The only matches for `hash(` are two prose
  mentions in `ids.py` and `read.py` docstrings **forbidding** it.
- All six `plan:` commits touch `WORKPLAN.md` and nothing else.
- Every batch commit is one concern; `5e8a40f` is the only behaviour change and
  it moves `SPEC.md` §3.6 and §4.0 in the same commit (spec-first satisfied).

### Why nothing is in the blocking bin

The bar was probed, not assumed:

- **Tracebacks.** S2 drove the CLI at a directory, a `chmod 000` file, invalid
  UTF-8 bytes, a closed stdin, an unknown adapter and a missing file; S3 drove
  `build` over empty, whitespace, `null` and absent span ids in three container
  shapes; S5 re-ran the digit-limit suite under `PYTHONINTMAXSTRDIGITS=0` and
  `=640` on two interpreters. Every failure was a documented refusal or a
  diagnostic. No traceback reached a user.
- **Invariants.** The one behavioural commit was checked against all seven:
  no semantic vocabulary added; both records of every probe reached a node or a
  diagnostic; every edge declares kind, warrant and basis honestly; output is
  byte-identical across three interpreters and three `PYTHONHASHSEED` values and
  under line reordering; no `NodeKind`, `EdgeKind`, `Payload` state, warrant or
  diagnostic code added or renamed; the schema artifact did not move.
- **The near miss, and why it is not blocking.** `5e8a40f` *repairs* an
  invariant-2/3 defect that was live on its parent: on the review's two-record
  input, `bed0ce2` gave `nodes ['', 'c'] edges [temporal] diags []` — an
  `explicit` parent edge the telemetry stated, gone with **no** diagnostic. That
  is fixed here. Its residue (§S3.1 below) is a SPEC over-statement, not an
  invariant breach.

---

## 1. S1 — `aed32a9` *tests: the census names its own figures, and every one needs a family*

Parent `0533951`. Plan commit `078131d` (WORKPLAN.md only).

### Verified

- **The guard works where it reaches.** `figure_names()` yields **34** whole-number
  figures at HEAD. A wrong value was planted at a durable-document citation site
  for every figure that has one — **32 plants, all RED**. Six figures have no
  citation site (grep proves it).
- **The five run-4 plants were green before this commit** and are red after.
  On `0533951` (8 families, `len(CENSUS_FIGURE_PATTERNS) == 8`) each of
  `139→939`, `135→935` and three `0→9` plants gave `47 passed`. At HEAD the
  current-value equivalents (`141→941`, `137→937`, three `0→9`) each give
  `1 failed`.
- **Both derivation gates bite.** Adding `invented_figure -> int` to `Census`
  fails `test_every_figure_the_census_computes_is_guarded_by_a_family` by name;
  adding `invented_fraction -> float` fails
  `test_the_census_computes_no_fractional_figure_without_a_stated_guard`.
  Breaking one family's regex fails
  `test_every_figure_family_still_reads_the_sentence_it_was_written_for`, and
  only that test.
- **`FRACTIONAL_FIGURES`** — the two seconds-valued gaps are pinned through
  `round(gap * 1e6)`; planting `136 µs → 936 µs` at `OQ:483` is RED.
- Suite numbers at the commit reproduce: `2438 passed, 2 skipped`, consistent
  with the body's "1 failed, 2437 passed" under one plant.
- `TASKS.md`'s `audit-R9` headline correction is present (`TASKS.md:10763-10779`).

### Findings

**1.1 [OPEN THREAD] Four live census figures in one `CHANGELOG.md` sentence are
unguarded.** *(re-verified by the orchestrator)* `CHANGELOG.md:942-944` is S3's
own recount sentence. Planting all four wrong and running the suite alone:

```
# worktree of HEAD
sed -i '942s|52/151 -> \*\*54/155\*\*|52/151 -> **854/955**|' CHANGELOG.md
sed -i '943s/\*\*141\*\* carrying a span id/**941** carrying a span id/' CHANGELOG.md
sed -i '943s/\*\*137\*\* trace-unique/**937** trace-unique/' CHANGELOG.md
sed -i '944s/50 -> \*\*52\*\*/50 -> **952**/' CHANGELOG.md
uv run pytest tests/test_doc_truth.py -q
# -> 50 passed in 1.52s
```

Adding a fifth plant to the **same sentence** — `308 → 908 timestamp literals` —
gives `1 failed, 49 passed`
(`test_every_corpus_figure_a_durable_document_asserts_is_the_census`,
`tests/test_doc_truth.py:2400`). So the scan reads the sentence; it simply has
no family for the spellings `N/M`, `N carrying a span id`, `N trace-unique` and
`N tracked *.jsonl`. This is the batch's own thesis — a guard narrower than its
headline — one sentence over.

> *TASKS.md:* Add figure families for the `N/M`, "N carrying a span id",
> "N trace-unique" and "N tracked `*.jsonl`" spellings used in `CHANGELOG.md`'s
> recount sentences, or rewrite those sentences into a spelling an existing
> family reads.

**1.2 [OPEN THREAD] `RETIRED_CENSUS_FIGURES` lets a present-tense sentence state
a wrong figure and stay green.** Two green plants:

```
sed -i '1351s/137 of the 155/135 of the 155/' OPEN_QUESTIONS.md   # -> 50 passed
sed -i '2909s/carries 52 /carries 64 /'       OPEN_QUESTIONS.md   # -> 50 passed
```

`WORKING_TREE_CENSUS` (`tests/test_doc_truth.py:1699`) is a hand-written
alternation over the *old* working-tree values only; it does not list `139`,
`135`, `151`, `50` or `64`. So the second half of the two-test division — "a
retired figure must be marked as history" — applies to nothing S3 retired. S3's
comment states this price for `52/151` alone.

> *TASKS.md:* Extend `WORKING_TREE_CENSUS` to every value in
> `RETIRED_CENSUS_FIGURES`, or derive it from that dict, so a retired figure
> asserted in a live sentence is caught by the history-marking test.

**1.3 [OPEN THREAD] `figure_names()` fails open on any container shape the walk
does not handle.** Adding a `Census` property typed `list[int]`,
`frozenset[int]`, `dict[str, dict[str, int]]` or `bool` leaves the suite at
`50 passed` — only `int`, `float`, `dict[_, int]`, `dict[int, _]`,
`tuple[int, ...]` and nested dataclasses are walked. The defect the commit
diagnoses ("a hand-written list cannot report what it omits") is relocated from
a list of families to a fixed list of handled type shapes, with no gate on it.

> *TASKS.md:* Make `_names` raise on an annotation shape it does not understand
> instead of returning silently, so a census figure in an unhandled container
> fails the derivation gate rather than vanishing from it.

**1.4 [OPEN THREAD] The site-free scan drops any paragraph containing a `>`
line.** `OPEN_QUESTIONS.md:1136-1137` (*"of the **155** tracked corpus records,
**141** carry a `span_id` and **137** of those are trace-unique"*) sits in a
63-line list item with an embedded blockquote, so
`test_every_corpus_figure_a_durable_document_asserts_is_the_census` never reads
it; the `141→941` plant is caught only by the site-listed test. Two durable
paragraphs are skipped this way today.

> *TASKS.md:* Drop only the blockquote lines from a paragraph instead of the
> whole paragraph in
> `test_every_corpus_figure_a_durable_document_asserts_is_the_census`, so a
> citation that shares a list item with a quote is still scanned.

**1.5 [OPEN THREAD] The body says "33 figures, 24 families"; the commit computes
34.** At `aed32a9`, `len(figure_names()) == 34` and
`len(CENSUS_FIGURE_FAMILIES) == 24`.

> *TASKS.md:* Correct the `audit-S1` figure count where it is repeated in
> `CHANGELOG.md`/`TASKS.md` — the census named 34 figures, not 33.

**1.6 [OPEN THREAD] Six of the 34 figures are cited by no durable document**, so
their families are proven only against example sentences the test itself
contains: `claims`, `claims.keys`, `timestamps.sibling_pairs`,
`timestamps.minimum_sibling_gap_us`, `captured_receipts.files_with_data_edges`,
`indented_exports[].lines`. `test_every_figure_family_still_reads_the_sentence_it_was_written_for`
checks a family against its own `example`, so it cannot distinguish a family
guarding a real citation from one guarding a sentence only the test contains.

> *TASKS.md:* Record in `tests/test_doc_truth.py` which families currently match
> no durable-document sentence, so a family that exists only for its own example
> is visible rather than indistinguishable from a working guard.

**1.7 [OPEN THREAD] Slot sets are unioned across scopes.** `timestamp literals`
accepts `{34, 308}`, `above the unit ceiling` accepts `{0, 10}`, and
`lines of an indented export` accepts the full 2×2 product. Every such plant
tried today was caught by the *site-listed* test, but at a site not on that list
a captured-scope sentence could state the whole-corpus number and pass.

> *TASKS.md:* Note in `stated_census_figures` that a multi-name slot accepts
> every scope's value, so a cross-scope citation is caught only where the
> site-listed test also covers it.

---

## 2. S2 — `bed0ce2` *docs: the bracket a caller matches is a spanweave code, not any bracket*

Parent `aed32a9`. Plan commit `ad0de3c` (WORKPLAN.md only). Documents only:
`CHANGELOG.md`, `README.md`, `SPEC.md`. `git diff aed32a9 bed0ce2 -- tests/` is
empty.

### Verified

- **The transcript reproduces byte-for-byte.**
  `uv run spanweave build /nope/not-a-trace.jsonl` → stdout empty, stderr exactly
  `spanweave build: [Errno 2] No such file or directory: '/nope/not-a-trace.jsonl'`
  (confirmed with `cat -A`), `exit=1`.
- **`tests/test_cli.py:630` did assert it all along.** On the parent:
  `assert printed.startswith("spanweave build: [Errno 2] "), printed`, inside
  `test_a_failure_the_library_did_not_raise_carries_no_code`. The suite encoded
  the behaviour while three documents stated its negation — the body's claim is
  true.
- **§3.10's set is genuinely closed, and the new positive rule survives
  probing.** `spanweave/errors.py` `ERROR_CODES` is 8 values matching §3.10's
  table, with a test asserting the match; no ad-hoc `code=` anywhere under
  `spanweave/`. Probes: a directory → `[Errno 21] Is a directory`; `chmod 000` →
  `[Errno 13] Permission denied`; invalid UTF-8 → `[adapter_unconfident]`, and
  under `--adapter openinference` a `malformed_record` diagnostic at exit 0;
  closed stdin → `[adapter_unconfident]`; `--adapter nope` → `[unknown_adapter]`.
  **No collision is possible**: the OS bracket always carries the literal
  `Errno ` prefix and a decimal, and every §3.10 value is a lowercase identifier.
- **The README transcript reproduces from a clean directory**, and the doc-truth
  fence test (`test_the_readme_shows_the_failure_line_the_cli_actually_prints`)
  reads the filename out of the fence, so the rename needed no test change.
- `make check` green at the commit; `pytest tests/test_cli.py tests/test_doc_truth.py`
  → `101 passed`.

### Findings

**2.1 [OPEN THREAD] The fourth site was missed, and it is in the shipped
source.** *(re-verified by the orchestrator)* `spanweave/cli.py:398-399` still
reads:

```
# keep. An `OSError` below prints no bracket -- it is the operating
# system's answer and has no code in that table to print.
```

— the exact sentence this commit spent three documents retracting, three lines
above the `print()` that emits `[Errno 2]`. Still present at HEAD
(`git log bed0ce2..HEAD -- spanweave/cli.py` is empty). The miss traces to
`WORKPLAN.md:152`, which enumerated only README:199, SPEC:1693 and CHANGELOG:344.
Repro: `grep -n "prints no bracket" spanweave/cli.py`.

> *TASKS.md:* Correct the `OSError` comment in `spanweave/cli.py` (~line 398) the
> same way the three documents were corrected: the CLI adds no bracket of its
> own, but Python's `OSError.__str__` opens with `[Errno N]`, which is not a
> `SPEC.md` §3.10 code.

**2.2 [OPEN THREAD] A test name still states the generalized claim that was just
retracted.** `tests/test_cli.py:610` is
`test_the_bracket_is_the_only_machine_readable_part_of_the_line`, and its body at
:619 does `printed[printed.index("[") + 1 : printed.index("]")]` — precisely the
naive extraction the CHANGELOG now says "extracted `Errno 2` from a missing
file". The body is safe (it only runs on a raised refusal); the name is a durable
statement of the rule in its false form.

> *TASKS.md:* Rename `test_the_bracket_is_the_only_machine_readable_part_of_the_line`
> to say what it asserts — the bracket on a *raised* refusal is one of
> `ERROR_CODES` — so no reader takes the name as the general rule.

**2.3 [OPEN THREAD] Nothing guards the corrected sentences.** `test_doc_truth.py`
recomputes the exit-code tables and runs the README's one transcript, but no test
touches the prose of SPEC §7's two bullets or the README paragraph. The stale
`cli.py` comment is the proof that an unguarded restatement survives a correction
pass.

> *TASKS.md:* Add a doc-truth check that the `[Errno 2]` failure line the CLI
> prints appears verbatim in every document that describes it, so the sentence
> cannot drift from the behaviour again.

**2.4 [OPEN THREAD] SPEC §7's parent sentence is contradicted by the bullets under
it and by `validate`.** `SPEC.md:1679` still says *"a command that fails prints
one line, `spanweave <command>: <the failure>`."* Two things falsify it: the
`hint:` case (which the child bullet itself documents as a second line), and
`validate`, which prints one line **per problem**, prefixed with the graph path
and no `spanweave <command>: ` at all. Repro:

```
printf '{"schema_version":"0.1"}' > g.json; spanweave validate g.json
# -> four lines of `.../g.json: missing top-level key '...'`, exit 1
printf nope > b.json; spanweave validate b.json
# -> `.../b.json: not valid JSON (Expecting value: line 1 column 1 (char 0))`, exit 1
```

Pre-existing, but this commit rewrote the bullets directly beneath it, and a
caller following the new positive rule literally finds no prefix on a `validate`
failure.

> *TASKS.md:* Correct `SPEC.md` §7 *Failures* so its opening sentence covers the
> shapes the CLI actually prints — `validate`'s path-prefixed, possibly
> multi-line output and the secondary `hint:` line — instead of promising exactly
> one `spanweave <command>: …` line.

**2.5 [OPEN THREAD] The README does not state the file's contents.** The fence
says `unrecognized.jsonl` "exists — it holds JSON no adapter recognizes", which
is not reproducible text; a reader must invent the bytes. It happens not to
matter (`{}`, `[]`, `{"hello":"world"}`, `not json at all` and an empty file all
give the identical line), but the fence is one step short of pasteable — the same
class as the §11 nit it fixes.

> *TASKS.md:* Show the one line `unrecognized.jsonl` contains
> (`{"hello":"world"}`, which is what the doc-truth test writes) so the README
> transcript is pasteable end to end.

**2.6 [OPEN THREAD] Two concerns in one commit, both declared.** F2 (the
three-document sentence) and the §11 README-transcript rename are separate review
findings touching different paragraphs. Both are docs-only and the body names
both, so this is a note rather than an objection to the diff.

> *TASKS.md:* Keep an unrelated review nit out of a correction commit even when
> both are documentation, or say in the subject that the commit carries two.

**2.7 [OPEN THREAD] The `audit-R16` CHANGELOG entry was amended in place.**
Lines 344-349 rewrite a historical entry, annotated *(corrected from "prints no
bracket" by the batch below)*. Honest and self-pointing, but the CHANGELOG no
longer records what `audit-R16` shipped; the reflow also leaves a stub line
(`in a comment inside \`spanweave/cli.py\``) alone on line 350.

> *TASKS.md:* Decide and record whether a past CHANGELOG entry may be amended in
> place, and reflow the `audit-R16` paragraph so no line is left orphaned
> mid-sentence.

---

## 3. S3 — `5e8a40f` *ids: an empty span id is no identity, so an empty parent loses nothing*

Parent `bed0ce2`. Plan commit `5ef03b3`. **The only behaviour change in run 5.**

### Verified

- **The defect it repairs was real.** On the review's two-record JSONL, in
  worktrees: pre-`audit-R10` gave
  `edges [('', 'c', PARENT, EXPLICIT, 'span.parent_span_id'), (temporal)]`;
  the parent `bed0ce2` gave `nodes ['', 'c'] edges [temporal] diags []` — the
  explicit edge gone with **no** diagnostic; HEAD gives
  `nodes ['sw_b1ea560c790200c4', 'c'] edges [temporal] diags []`. The reviewer
  recovered the body's exact derived id by brute-forcing `derive()` over
  candidate records.
- **Three container shapes, hand-written, nothing dropped.** Flat OpenInference
  JSONL, flat OTel GenAI JSONL and a real `ExportTraceServiceRequest` envelope
  each give 2 nodes, 1 temporal edge, **0 diagnostics**, no node named `""`, and
  `raw.source` carrying `"span_id": ""` / `"parent_id": ""` verbatim. Both
  records become nodes, so losslessness holds in all three.
- **OTLP `spanId` spellings.** absent → `sw_5d19f66d4452d002`; `null` →
  `sw_72748ccc7686d5f8`; `""` → `sw_3d9b3113d1dee577`; each a derived id, no
  diagnostic, no `""` node. (Base64 of zero bytes *is* `""`, so it is the same
  case, not a fourth.) `"   "` → node id `'   '`, as SPEC §3.6 says.
- **Adversarial identity.** Two records both `span_id: ""` with different content
  → two distinct derived ids, nothing collides. Two *identical* such records →
  collapsed by the reader with a `duplicate_record` diagnostic — reported, not
  silent.
- **Determinism.** Byte-identical output under `PYTHONHASHSEED` unset/1/12345 in
  three separate interpreters (`cmp` clean). Line-reversed input is identical
  except `meta.source_digest`, which is a digest of the input bytes and genuinely
  changed. `tests/test_gates.py` → 47 passed.
- **Tests are red on the parent** (worktree `bed0ce2`, the commit's test files and
  fixture checked in): `test_no_node_is_named_by_the_empty_string[both]`
  (`assert '' not in {'', 'c'}`),
  `test_an_empty_span_id_keys_on_its_content_like_an_absent_one[both]`,
  `tests/test_read.py::test_an_empty_span_id_is_no_identity_in_the_export_container_too`,
  and both `empty_ids` conformance parametrizations.
- **Corpus expectations unmoved.** Only *new* `empty_ids/expected/*` files appear;
  no existing `expected/` file is touched.
  `git grep '"span_id": *""\|"spanId": *""' -- fixtures capture` outside
  `empty_ids` → **zero hits**, so "no tracked record states an id empty" holds.
  `make conformance` at HEAD → **580 passed, 2 skipped**, unmoved.
- **No model change.** `git diff --name-only bed0ce2 5e8a40f -- spanweave/` is
  exactly the three files; `model.py`, `diagnostics.py`, `graph.py`, `build.py`
  and `schema/` are untouched; `tests/serialized_shape.json` is not in the diff.
- **The census recompute is exact.** All seven figures re-measured with
  `uv run python -m tests.corpus_census`: 54/155 ✓, 141 ✓, 137 ✓, 308 ✓, 28/25 ✓,
  52 ✓, README 28/23 ✓. The four corpus-size pin bumps are present and each is
  annotated beside the previous bumps.
- **Commit-time counts exact**: at `5e8a40f`, `2486 passed, 2 skipped` and
  `580 passed, 2 skipped`.

**On the "unstatable" edge — verdict: fair, no invariant breached.** The
`('', 'c')` edge cannot be stated once `""` is not an identity, and what the
parent lost *is* recoverable: the built graph's `raw.source` carries
`"span_id": ""` on the root and `"parent_id": ""` on the child, verbatim.
Invariant 2 asks that every input record become a node or a diagnostic — both do.
Invariant 3 governs edges that exist, and every edge present declares kind,
warrant and basis honestly. The residual — that a consumer reading only the edge
set cannot distinguish "root by proto3 default" from "reference normalized away",
with no diagnostic marking the normalization — is `audit-R10`'s documented
decision restated in SPEC §4.0, not something S3 introduced.

### Findings

**3.1 [OPEN THREAD] `links[].span_id` is a third reference end the new rule was
not applied to, so SPEC §3.6's new absolute is falsifiable by an input.**
*(re-verified by the orchestrator)* `SPEC.md:390` now says **"The empty string is
not a span id — at either end of a relation."** Both adapters' `_links()` still
use `_as_str`, not `span_ref` (`openinference.py:576`, and the genai twin).
Repro:

```
cat > link_empty.jsonl <<'EOF'
{"trace_id":"t1","span_id":"a","name":"agent.run","start_time":1000.0,"end_time":1003.0,"attributes":{"openinference.span.kind":"AGENT"}}
{"trace_id":"t1","span_id":"","name":"tool.lookup","start_time":1000.5,"end_time":1001.0,"links":[{"span_id":""}],"attributes":{"openinference.span.kind":"TOOL","tool.name":"lookup"}}
EOF
uv run spanweave build link_empty.jsonl
```

Observed:

```
nodes ['a', 'sw_18d377253751cb4c']
edges [('sw_18d377253751cb4c', '', 'link', 'explicit', 'span.link'), ('a', 'sw_18d377253751cb4c', 'temporal', 'derived', 'sibling start_time ordering')]
diags []
```

An `explicit` edge whose `dst` is `""` — and, because no node can be named `""`
any more, now *guaranteed* dangling where before it resolved. Not an invariant
breach: SPEC §4.0 permits a link pointing outside the trace, and kind, warrant
and basis are honest. But the SPEC sentence overclaims, and `ADAPTERS.md`'s new
"fill them with `span_ref`, `parent_ref`" guidance omits the link field.

> *TASKS.md:* Decide whether a link whose `span_id` is `""` is a stated reference
> or no reference, apply `span_ref` (or narrow `SPEC.md` §3.6's "at either end of
> a relation") accordingly, and cover it with a fixture.

**3.2 [OPEN THREAD] The body's three-row before/after table does not reproduce as
printed.** On the single input that yields the quoted `sw_b1ea560c790200c4`,
row 1 has **two** edges (parent *and* temporal), not the one shown, and row 2 is
`edges [temporal]`, not `edges []`. Rows 1–2 look copied from the run-4 review,
whose no-timestamp JSONL gave `diags ['missing_timestamp','missing_timestamp']` —
contradicting the same row's `diags []`. The load-bearing claim (explicit parent
edge silently gone, zero diagnostics) does reproduce; the table does not.

> *TASKS.md:* Re-measure `5e8a40f`'s before/after table against a single named
> input and correct the edge and diagnostic lists, or state which input each row
> was taken on.

**3.3 [OPEN THREAD] "Tests, each watched failing first" is not true at test
granularity.** On `bed0ce2`, 4 of the 8 new `test_adapters.py` parametrizations
**pass**: `test_an_empty_span_id_was_read_and_is_not_reported[both]` and
`test_exactly_the_empty_span_id_and_no_other_rendering[both]`
(`4 failed, 4 passed, 214 deselected`). They are legitimate boundary guards, but
they were never red.

> *TASKS.md:* Say which of a batch's new tests are red-on-parent and which are
> boundary guards, rather than asserting every one was watched failing.

---

## 4. S4 — `51da70e` *packaging: the citation guard sees a directory, and says what it cannot see*

Parent `5e8a40f`. Plan commit `51ca482`. Touches `CHANGELOG.md`,
`pyproject.toml`, `tests/install_check.py`, `tests/test_acceptance.py`. Nothing
under `spanweave/`; no SPEC section moves.

### Verified

- **Plant A red.** `/reviews` dropped from
  `[tool.hatch.build.targets.sdist].include` →
  `uv run python -m tests.install_check` **exit 1**,
  `FAILED: 1 check(s) — ['sdist: ships every tracked path its own documents cite']`,
  naming `reviews/ — cited by CHANGELOG.md, TASKS.md` beside the review files.
  At the commit's own tree the citer list is exactly the body's.
- **Plant B red.** `/.github` dropped → **exit 1**, naming
  `.github/ — cited by CHANGELOG.md, OPEN_QUESTIONS.md, TASKS.md, reviews/2026-09-11-run4.md`
  and `.github/workflows/ci.yml`.
- **The generalization isolates — it is not a no-op.** With `/.github` dropped
  *and* every `.github/workflows/…` citation reworded out of all shipped `.md`:
  the parent's `CITED_PATH` gives **exit 0**,
  `install-check green: 33 checks, 4 plants held` — an sdist with no `.github` at
  all ships unseen; HEAD's `CITED_PATH` on the identical tree gives **exit 1**.
- **"110 candidates, seven top-level directory citations" reproduces at the
  commit**: 110 distinct candidates and exactly
  `['.github/','capture/','examples/','fixtures/','reviews/','spanweave/','tests/']`.
  (At today's HEAD the same measurement gives 109 and the same 7 — the tree
  moved; the figure was right when written.)
- **`_unresolved_citations` is genuinely tested** by two `test_acceptance.py`
  functions; the commit's hunk applied to a `5e8a40f` worktree gives
  `3 failed, 1 passed` (`AttributeError: module 'tests.install_check' has no
  attribute '_unresolved_citations'`). The one that passes on the parent is the
  limit pin for a slashless root name — correct, it pins a limit that already
  held.
- **Both stated limits are real, and planted.** (a) `/Makefile` removed from the
  include with `` `Makefile` ``/`` `LICENSE` ``/`` `tests` `` cited in `TASKS.md`
  → green, `Makefile in sdist: False`. (b) `/reviews` replaced by a single member
  file → the report names the three *files* and does **not** name `reviews/`.
- **The widening costs nothing in silence and loses no reach.** Old vs new over
  the same documents: 102→109 candidates at HEAD, 103→110 at the commit; added =
  the 7 directory citations, **all checked**; `added-and-skipped: []`; skipped
  count unchanged; `lost by widening: []`. Removing each of the 14 top-level
  include entries in turn found **no** case where the old logic reported
  something the new one does not.
- **The dropped figure.** `38 such candidates were counted while writing this` is
  present at `5e8a40f:tests/install_check.py:301` and absent at `51da70e`; its
  remaining occurrence is `CHANGELOG.md:905`, removed by S5 — the deferral is
  accurate.
- `make check` and `make install-check` green at HEAD with the body's numbers.

### Findings

**4.1 [OPEN THREAD] The body's citer list for plant B is short by one.** At
`51da70e` the check reports
`.github/ — cited by CHANGELOG.md, OPEN_QUESTIONS.md, TASKS.md, WORKPLAN.md`;
the body says "cited by OPEN_QUESTIONS.md, TASKS.md, WORKPLAN.md". The missing
citer is the commit's *own* new CHANGELOG entry — the figure was measured before
that entry was written and never re-measured after.

> *TASKS.md:* Re-measure and correct the `.github/` citer list in `51da70e`'s
> recorded plant output, which omits `CHANGELOG.md`.

**4.2 [OPEN THREAD] "the **single** full-path citation of `.github/workflows/ci.yml`"
is false, and it made the isolation harder to reproduce than the text implies.**
At `51da70e` two shipped documents cite that path in a code span
(`CHANGELOG.md`, `reviews/2026-09-11-run3.md`) and at HEAD three (plus
`TASKS.md`). Reproducing the old-pattern PASS required rewording all of them.

> *TASKS.md:* Fix the CHANGELOG's "the single full-path citation of
> `.github/workflows/ci.yml`" — three shipped documents cite it, and the
> isolation requires rewording all of them.

**4.3 [OPEN THREAD] The directory rule is planted only at unit level.** The
commit's own thesis is "a reach sentence nothing plants against is how this guard
came to overstate itself", yet `install_check`'s four self-plants
(`missing-module`, `outside-file`, `path-leak`, `readme-decoupled`) still contain
none that omits a whole directory from `[tool.hatch.build.targets.sdist].include`.
Nothing in CI exercises pyproject → sdist → report for the defect the check exists
for; both plants above had to be run by hand.

> *TASKS.md:* Add a fifth `install_check` plant that drops a directory from the
> sdist allowlist and asserts the citation check is the one that fails, so the
> guard's own defect is under the gate rather than under a docstring.

---

## 5. S5 — `4727a46` *docs: the figure names the magnitude it was measured at, and the counts recount*

Parent `51da70e`. Plan commit `465a027`. Touches `CHANGELOG.md`,
`OPEN_QUESTIONS.md`, `SPEC.md`, `TASKS.md`, `tests/digit_limit.py` (docstring
only — no assertion moved). Nothing under `spanweave/`.

### Verified — every figure re-measured, none copied

- **F5.** `math.ulp(1.7e18) = 256.0`, `math.ulp(1.7e9)*1e9 = 238.4185791015625`.
  Body exact. The argument survives at both magnitudes:
  `float(1700000000100000000) == float(1700000000100000100)` and
  `1.7e9 == 1.7e9 + 1e-7` are both `True`.
  Site sweep: `SPEC.md:162` (inside §3.1, so §7's new "(§3.1's figure)"
  resolves), `SPEC.md:1686`, `ADAPTERS.md:180`, `CHANGELOG.md:500`/`:659`, both
  adapters and the `timestamp_units` scenario all say 256 ns at epoch-nanosecond
  magnitude. `OPEN_QUESTIONS.md` carries exactly **four** 238-figures
  (`:447`, `:589`, `:620`, `:3206`) — count confirmed, attachment correct.
- **F6.** `wc -l fixtures/conformance/otlp_container/dialects/openinference.json`
  → **328**. `spanweave inspect` on it → `nodes: 4 (agent 1, llm 2, tool 1)`,
  `edges: 7`, `diagnostics: 2 — unmapped_attributes: 2`, zero `malformed_record`.
  Body exact. **328 is gated**: `test_doc_truth.py:1811` pins it against
  `indented.lines_that_are_not_json`, recomputed from `git ls-files`; no literal
  `328` anywhere in `tests/`. **"Never true of that file"** holds: in a worktree
  at `ff05b2d` (the commit that *added* the fixture) the same command gives the
  identical 4/7/2. The dating idiom really is applied — the heading now reads
  *"(a) What a checkout did when this memo was written, measured (2026-09-10)"*
  with a provenance paragraph naming `ff05b2d` and `67d788d`.
- **F7.** Re-run on `1e121d2`: `PYTHONINTMAXSTRDIGITS=0` → `7 failed`
  (**7 ids / 6 functions**, one function parametrized over both adapters);
  `=640` → `2 failed` (**2 ids / 1 function**, `ValueError: Exceeds the limit
  (640 digits)`). **Byte-identical under CPython 3.12.3 and 3.14.6.**
  **Disjoint is literal**, not merely additive: the `=640` function appears in
  none of the `=0` ids. 7+2=9, 6+1=7. Both the docstring and the `audit-R14`
  entry now state population and configuration.
- **The counting nits recount.** `git grep -l resourceSpans -- fixtures capture
  examples` → **3** files (two `otlp_container` renderings + the scenario note),
  and the same three at `ff05b2d`, so "three since F2" holds.
  `grep -c '^- \*\*audit-R' TASKS.md` → **19**, ids R1–R19 each once, with
  exactly one (`audit-R18`) never-run → **eighteen ran**; "seventeen" was wrong.
  `38` is gone: `git grep -n "scenario-relative"` leaves only S5's own record of
  the drop and the now-numberless R15 entry. The two prose `R3`s are now
  `audit-R3`; the launch-checklist `R3`s are correctly untouched.
- **Old-value sweep, whole repo.** `~238 ns`, `"seventeen of which ran"`,
  `"five tests"`, `"two that carry it"` survive **only** in CHANGELOG/TASKS
  registry rows recording what an earlier batch asserted, and in `reviews/`
  archives. **No live uncorrected claim.**
- `uv run pytest tests/test_doc_truth.py -q` → `50 passed`; full suite
  `2490 passed, 2 skipped`; conformance `580 passed, 2 skipped`.

### Findings

**5.1 [OPEN THREAD] F5's own defect survives one site the batch did not sweep.**
`OPEN_QUESTIONS.md:594` reports the falsifying scan as *"13 sibling pairs, **0**
under 256 ns"*, five lines below the same paragraph's criterion *"finer than
238 ns"* (`:589`). Measured: all 34 captured timestamp literals are ~`1.787e9`
(0 above 1e11) — seconds magnitude, where `math.ulp(1.787e9)*1e9 = 238.42`. The
conclusion is unaffected (minimum gap 136 µs), but the threshold names the 1.7e18
figure over 1.7e9 data: the exact inversion F5 reported. Nothing gates it —
`test_doc_truth.py`'s §2(c) regex stops at `sibling pairs`.

> *TASKS.md:* Restate `OPEN_QUESTIONS.md` §2(c)'s "0 under 256 ns" as 238.42 ns,
> the ULP at the seconds magnitude the captured literals actually sit at.

**5.2 [OPEN THREAD] The batch adds a fresh hand-asserted measurement with no
doc-truth binding.** §16(a)'s new provenance note states *"**4** nodes … seven
edges … two `unmapped_attributes`"* in a file whose neighbouring figures are all
census-derived. A future adapter change makes it stale silently — the class this
series exists to close.

> *TASKS.md:* Bind §16(a)'s provenance-note node/edge/diagnostic counts to a
> recomputed build of the cited fixture in `tests/test_doc_truth.py`, or restate
> them as a dated measurement with no present tense.

**5.3 [OPEN THREAD] Two lines left over-length.** `CHANGELOG.md:1055` at **113**
chars (the `38`-removal hunk) and `TASKS.md:10824` at **88** (the `audit-R17`
rename hunk), against the file's ~79-char norm. No gate catches it; run-3's R5
finding 6 already filed "four over-long lines" as a nit.

> *TASKS.md:* Re-wrap the two lines this batch left over-length
> (`CHANGELOG.md:1055`, `TASKS.md:10824`) to the surrounding 79-column norm.

**5.4 [OPEN THREAD] The id-collision carve-out undercounts what it exempts.**
`TASKS.md:10501` exempts the run-3 table with *"the review's spelling … is kept as
it wrote it (`R1`, `R4`, `R5`)"*, but that table also carries bare `R2` (§4) and
`R3` (§7 ×4) as section labels, plus bare `R12` and `R7` inside the F6 row. The
commit body itself enumerates five — so the document's own count is two short of
the body's, in the batch whose subject line is "the counts recount".

> *TASKS.md:* Correct `TASKS.md`'s run-3-table carve-out parenthetical to name
> every bare id the table keeps (`R1`, `R2`, `R3`, `R4`, `R5`, and the `R7`/`R12`
> inside the F6 row), or drop the enumeration.

**5.5 [OPEN THREAD] A stale present-tense docstring in the module the F6 fix leans
on.** `tests/corpus_census.py:290`'s `IndentedExport` docstring still says
*"§16(a) states *46 `malformed_record` diagnostics and 0 nodes*"*; §16(a) has said
**328** since `67d788d`, and the "0 nodes" half is what F6 just dated as
never-true. Pre-existing, not introduced here.

> *TASKS.md:* Re-tense `tests/corpus_census.py`'s `IndentedExport` docstring to
> say what §16(a) said before `67d788d` rather than what it states now.

---

## 6. S6 — `02c79b4` *docs: a sentence about a guard gets the same adversarial read as the guard*

Parent `4727a46`. Plan commit `64b4f57`. Touches exactly `CHANGELOG.md`,
`README.md`, `SPEC.md`, `TASKS.md`. Nothing under `spanweave/`, `tests/` or
`fixtures/`; `tests/serialized_shape.json` and every `expected/graph.json`
unmoved. Docs-only, so "new test red on parent" does not apply.

### Verified

- **§4 R8 probes reproduce exactly.**
  `openinference._operation({'tool.name': 7, 'llm.model_name': 'm'}, set())` →
  `('m', 'm')`, consumed `['llm.model_name']`;
  `otel_genai._operation(NodeKind.TOOL, {'gen_ai.tool.name': 7, 'gen_ai.request.model': 'm'}, set())`
  → `('m', 'm')`.
  Single unreadable keys each give `(None, None)`, consumed `[]`. In a built
  graph, `{'tool.name':7,'llm.model_name':'m'}` → `operation='m'`,
  `attributes['model']='m'`, diagnostics `[('unmapped_attributes', ['tool.name'])]`
  — the unreadable key costs nothing and draws no other diagnostic.
  **Exhaustively**: all 3³ OpenInference and all 3² × 3-kind genai combinations
  compared against the same map with the unreadable keys deleted → **0
  violations**.
- **§7 R12 — seven keys, named.** OI `input.mime_type`, `output.mime_type`,
  `openinference.span.kind`; genai `gen_ai.operation.name`, `gen_ai.tool.name`,
  `gen_ai.request.model`, `gen_ai.tool.call.id`. "Six, three per dialect" was
  wrong: genai had four.
- **The corpus walk reproduces all five figures**, with a method written blind
  (`git ls-files '*.json' '*.jsonl'`, counting each key both as a plain object key
  and as an OTLP `KeyValue`): at `f03de6b`, six keys → **221 / 51 / 0
  non-string** (the entry's original figure, reproduced exactly); the seventh
  alone → **76 / 25 / 0**; all seven → **297 / 52 / 0**. At HEAD: **78 / 26** and
  **302 / 54**, still 0. Every figure exact.
- **Grammar fix landed.** `SPEC.md:559` now reads "The timestamps are not the only
  fields **it** holds for."
- **§9 R14 pointers landed and resolve.** `SPEC.md:1195-97` and `README.md:169-172`
  both point at §5.3 by its title, which matches `SPEC.md:1228` verbatim; §5.1 →
  §5.2 → §5.3 is genuinely two subsections down. The condition: **the
  interpreter's integer-string digit limit (`sys.get_int_max_str_digits`) is an
  input to the graph.** No other site over-claims —
  `AGENT.md:452`, `CONTRIBUTING.md:37` and `DESIGN.md` §4 state determinism as a
  rule list, `pyproject.toml` and `--help` use it adjectivally, `CONTRACTS.md`
  has no guarantee sentence.
- **The `CLAUDE.md` invariant-4 tension is real but disclosed**, registered at
  HEAD as `TASKS.md` open thread **23** and flagged as an `AGENT.md` halt point.
  Not a finding — it is in the durable place, not silently left.
- **§10 R15 illustration verified live.**
  `fixtures/conformance/span_links/dialects/otel_genai.jsonl` is not in
  `git ls-files` and does not exist on disk; `install_check._cited_paths` returns
  it as a candidate cited by `TASKS.md`, and `_unresolved_citations` returns `{}`
  — seen and skipped, as claimed.
- **Three edges planted, the fourth correctly excused.**
  `test_the_citation_guard_reports_a_cited_directory_the_sdist_omits`,
  `…_cannot_see_a_root_name_written_without_a_slash`,
  `…_does_not_check_every_file_of_a_cited_directory`. The fourth (accuracy) is
  genuinely unplantable — a test could only assert the guard passes on an
  existing-but-misdescribed citation, indistinguishable from the existence check
  it already does.
- `uv run pytest tests/test_doc_truth.py tests/test_acceptance.py -q` →
  **76 passed**.

### Findings

**6.1 [OPEN THREAD] The exact over-statement this commit removed from SPEC §3.7
survives verbatim in two adapter docstrings.** *(re-verified by the
orchestrator)* `spanweave/adapters/openinference.py:469-470` and
`spanweave/adapters/otel_genai.py:606-607` both read *"a name the adapter cannot
read as a string decided nothing -- `operation` and `model` stay `None`"*. The
body asserts §3.7 was rewritten "at the precision the `_operation` docstrings
already carried" and that the R8 phrase "had none" as a second site; both are
false — the docstrings carry the *imprecise* form, and the probe falsifies them
(`{'tool.name': 7, 'llm.model_name': 'm'}` → `('m','m')`, not `(None, None)`).
The body took the run-4 review's characterisation at face value, which is the one
thing the batch said it would not do.

> *TASKS.md:* Correct the `_operation` docstrings in both adapters so they state
> the per-key reading `SPEC.md` §3.7 now states, instead of "`operation` and
> `model` stay `None`".

**6.2 [OPEN THREAD] The new §3.7 clause is itself falsifiable.**
*(re-verified by the orchestrator)* §3.7 now says **"the two are `None` only where
no readable name is left"**. Repro:

```
otel_genai._operation(NodeKind.LLM, {'gen_ai.tool.name': 't'}, set())   # -> (None, None)
openinference._operation({'tool.name': 't', 'llm.model_name': 7}, set()) # -> ('t', None)
```

In the first, a readable name is not only left but *consumed*; in the second,
`model` is `None` with a readable name present. §3.7 presents itself as "the
adapter's rule, not one key's", so scoping the clause to the three OI keys in its
parenthesis does not save it.

> *TASKS.md:* Narrow `SPEC.md` §3.7's "the two are `None` only where no readable
> name is left" to say `None` means no readable name **for that field**, since a
> readable name that loses its field leaves both `None` in the genai dialect.

**6.3 [OPEN THREAD] The `TASKS.md` 3.6 illustration depends on a file staying
absent, and nothing pins it.** If
`fixtures/conformance/span_links/dialects/otel_genai.jsonl` is ever written — the
memo at `TASKS.md:3856` proposes exactly that — "a rendering proposed by a memo
above and never written" becomes false, the guard's behaviour on it changes from
skip to resolve, and no test notices;
`test_a_durable_document_cites_no_untracked_scratch_path` only scans `patches/`.

> *TASKS.md:* Pin the `TASKS.md` 3.6 untracked-citation illustration with a
> doc-truth assertion that the path it names is still untracked, or reword it so
> writing the fixture does not falsify the sentence.

**6.4 [OPEN THREAD] "the reach they leave has **four** edges" undercounts the
limits the cited docstring discloses.** It also names **non-markdown citers**:
only `.md` members are read (`_sdist_documents`), which `TASKS.md`'s "a document
the sdist ships" does not exclude. A docstring or comment in a shipped `.py`
citing a missing path is invisible to the guard and unmentioned in the rule.

> *TASKS.md:* Either say the citers are the shipped markdown documents, or count
> the non-markdown-citer limit among the edges in `TASKS.md` 3.6's `audit-R15`
> amendment.

---

## 7. S7 — `67c7642` *docs: close the audit-fix series a third time, and account for every run-4 finding*

Parent `64b4f57`. HEAD of `audit-fixes`.

### Verified

- **The decision text moved verbatim.** Extracting the sole §3 row from
  `git show 64b4f57:WORKPLAN.md` and the `2026-09-12 | audit-S3` row at
  `TASKS.md:10972`, applying exactly `| S3 |`→`` | `audit-S3` | `` and
  `R10's rule`→``​`audit-R10`'s rule``: **`diff` is empty.** No rewrap, no
  punctuation change, no dropped clause (422 B → 438 B, the delta being the two
  id spellings).
- **The sha256 matches.** `sha256sum reviews/2026-09-11-run4.md` =
  `f7349552edd04ad105abac7cca7083e95ec2a67d24dd6c2444e0b3578ced6aa3` — the cited
  prefix `f7349552edd04ad1` is correct.
- **32 rows, no orphan — confirmed by independent enumeration.** Reading
  `reviews/2026-09-11-run4.md` cold and numbering every finding and nit by
  section: §2 F1–F7 (7); §3 1; §4 1; §5 2; §6 1; §7 3+1; §8 1; §9 1; §10 2+1;
  §11 1; §12 1+1; §13 2; §14 2; §15 items 1–6 + closing observation = **32**.
  `grep -c '^| '` over the TASKS.md map = 33 including the header → **32 data
  rows**, matching section for section. **No finding without a row; no row
  without a finding.**
- **Threads 19–23 present with live evidence.** 19: `ADAPTERS.md:108` "not
  optional"; `seam.py:148-150` `__post_init__` normalizes only `attributes` /
  `call_names`; `grep -c unreadable_fields ADAPTERS.md SPEC.md` = **0/0**.
  20: §17 figures on no named-site list. 21: `RETIRED_CENSUS_FIGURES` at
  `tests/test_doc_truth.py:2212` with its standing-cost comment at :2223-2226.
  22: the run-3 table really does write bare `R1/R3/R4/R5`, disclosed in its
  preamble. 23: `CLAUDE.md:39`, `SPEC.md:1228`, `ENVIRONMENT.md:31`,
  `AGENT.md:357` — all quoted accurately. Threads 10/11/13/16(g) consistent;
  thread 13 does carry `status_message … _as_str at :552 / :739`, both adapters.
- **`WORKPLAN.md` is gone and nothing still points at it.**
  `git ls-files | grep -i workplan` empty. Surviving mentions: `CHANGELOG.md` 4,
  `OPEN_QUESTIONS.md` 18 (all inside §10–§17 memos, covered by the preamble at
  :16-26, edited in this commit), `TASKS.md` 14 (past tense + three `git show`
  pointers), `reviews/*` 26 (verbatim archives). **All historical; none a live
  instruction.** No reference in `.github/workflows/ci.yml`, `Makefile`,
  `AGENT.md`, `CONTRIBUTING.md` or `pyproject.toml`.
- **README and doc-truth are consistent with the deletion.** The `WORKPLAN.md`
  Documents row is deleted; the table lists 16 docs + README = the 17 root `.md`
  files. The exclusion in `durable_documents()` was **removed**, not left
  dangling — no dead reference to a nonexistent file.
  `uv run pytest tests/test_doc_truth.py -q` → **50 passed**.
- **§0 identity holds, and further than claimed.** §0 extracted from `fcc842d^`,
  `b742177`, `64b4f57` and `15cfab5` is **byte-identical in all four**,
  135 lines / 7119 bytes.
- **Batch shas resolve** to the S1–S6 subjects named; `audit-R7` `b091904`
  correct.
- **Thread 10 vs CI is accurate.** `ci.yml:29` matrix is
  `["3.11","3.12","3.13","3.14"]` — a plain `3.14` row, not `3.14-dev`, no
  `continue-on-error`; `pyproject.toml:18-21` carries the four matching
  classifiers, and `tests/test_acceptance.py` asserts matrix == classifiers.
- Nothing under `spanweave/` touched; `tests/serialized_shape.json` unmoved.

### Findings

**7.1 [OPEN THREAD] A durable sentence that is checkable and wrong, inside the
entry the close is accounting for.** *(re-verified by the orchestrator)*
`CHANGELOG.md:1018` states *"On 3.11.15, 3.12.3 and 3.13.14 the two ceilings
**coincide** (991/989, 9997/9996, 9998/9997)"* — every one of those three pairs is
non-coincident in the sentence that calls them coincident, and all three
contradict the R13 table at `CHANGELOG.md:555` and `SPEC.md:1556`
(992/992, 9997/9997, 9998/9998), which the run-4 review §13 independently
confirmed as "exact". Measured here on CPython 3.12.3:

```
uv run python -c "..."   # bisect json.loads / json.dumps nesting ceilings
# -> 3.12.3 loads: 9997 dumps: 9997
```

so the table is right and `:1018` is wrong. Thread 10
(`TASKS.md:11369-11376`) records only the 3.11.15 half and dispositions it as
*"±2 on a quantity this thread's rule already refuses to promise … recorded rather
than rewritten"*, which mischaracterises it: a cross-machine ±2 does not explain a
pair contradicting the word beside it, or two different pairs for the same
interpreter in the same file.

> *TASKS.md:* Reconcile `CHANGELOG.md:1018`'s three ceiling pairs with the R13
> table at `:555` and `SPEC.md:1556` — the sentence says "coincide" and none of
> its three pairs do.

**7.2 [OPEN THREAD] The close's own sha is recorded nowhere.** `audit-S7`'s row
(`TASKS.md:10941`) and the run-5 range (`:10469`) both read *"this commit"*, so
`67c7642` appears nowhere in `TASKS.md`. That spelling was survivable for
`audit-R7` only because a later close — this one — filled in `b091904`; this close
is the one that stands, so nothing will fill this one in, and the registry's one
unresolvable batch reference is its own.

> *TASKS.md:* Record `audit-S7`'s commit sha in its `TASKS.md` bullet and in the
> run-5 range, as every other batch row does.

**7.3 [OPEN THREAD] The byte-for-byte archival claim is unverifiable from a clean
checkout.** *"byte-for-byte from the untracked scratch drop … equal both sides,
`cmp` clean"* — `patches/` holds only 113 `.patch` files, no review drop, and no
`REVIEW-2026-09-11*` exists on the box. The digest itself reproduces and matches
the cited prefix, so the claim is not contradicted, only unfalsifiable — the same
state `audit-R2` was criticised for.

> *TASKS.md:* State in the archive header which scratch path a review was copied
> from and its digest, so a later reader can tell a verified copy from an asserted
> one.

**7.4 [OPEN THREAD] Two reflowed lines left over-length.** `CHANGELOG.md:635`
(90 chars, with `:636` short after it) and `OPEN_QUESTIONS.md:24` (88). Thread
16(f) already owns "four over-long lines"; this adds two. Same family as §5.3.

> *TASKS.md:* Rewrap `CHANGELOG.md:635-637` and `OPEN_QUESTIONS.md:24` to the
> ~78–82 norm.

---

## 8. Summary

| Batch | Commit | Blocks PR | Open threads |
|---|---|---|---|
| S1 | `aed32a9` | 0 | 7 |
| S2 | `bed0ce2` | 0 | 7 |
| S3 | `5e8a40f` | 0 | 3 |
| S4 | `51da70e` | 0 | 3 |
| S5 | `4727a46` | 0 | 5 |
| S6 | `02c79b4` | 0 | 4 |
| S7 | `67c7642` | 0 | 4 |
| **Total** | | **0** | **33** |

### The three that most repay attention

1. **§1.1** — four live census figures in `CHANGELOG.md:942-944` stay green under
   a wrong-value plant, in the sentence S3 wrote to record its own recount, while
   a fifth figure in the same sentence goes red. It is the batch's thesis
   reproduced one sentence past the batch.
2. **§6.1 and §2.1** — the same defect twice: a sentence corrected in the
   documents and left standing in the shipped source
   (`spanweave/adapters/*.py:_operation` docstrings; `spanweave/cli.py:398`). Both
   batches enumerated their sites from a review report rather than from a grep.
3. **§7.1** — `CHANGELOG.md:1018` calls three ceiling pairs "coincide" when none
   of them do, and contradicts the same file's own table. Checkable, wrong, and
   currently dispositioned as a rounding note.

### What was probed and found clean

No traceback on any interpreter; no `hash()`, clock, randomness or network under
`spanweave/`; `tests/serialized_shape.json` unmoved across the range; corpus
expectations unmoved by `make conformance`; determinism holding across three
interpreters, three `PYTHONHASHSEED` values and reordered input; every plant run
against S4's citation guard red; every run-4 review finding carrying a
disposition (32 rows, independently enumerated, no orphan); §0 of `WORKPLAN.md`
byte-identical across all four commits that carry it; and the empty-id decision
text moved with an empty `diff`.

### Not checked

- Only CPython 3.12.3 throughout, except S5's digit-limit re-run and S2's CLI
  probes, which also used 3.14.6. The `[Errno N]` byte string was confirmed on
  one interpreter.
- The run-4 review's original untracked scratch bytes no longer exist, so §7.3's
  archival provenance could not be checked, only its digest.
- S3's body table could only be reconstructed by brute-forcing `derive()` for the
  quoted id; the reconstruction matches exactly but is not provably the author's
  file.
- The `78 / 27` candidate figures in S5's body were deliberately out of scope.
- No plants were attempted into `reviews/` or `patches/`, which
  `durable_documents()` excludes by design.
