{
  "_note": "Dialects that CANNOT render this scenario, each with a reason (FIXTURES.md 4.3). A renderable:false is an INVITATION TO CHECK THE REASON against observed output, not a settled fact.",
  "otel_genai": {
    "reason": "This scenario's canonical graph pins kind: chain on s1, and the otel_genai adapter maps no gen_ai.operation.name value to NodeKind.chain. The links themselves remain fully expressible -- OTel span links are a record-level `links` field of the span data model, identical in both dialects and read by both adapters, and fixtures/captured/genai_workflow.jsonl now proves the otel_genai adapter reads one for real, producing an EdgeKind.link edge with basis span.link. What is still lost is not link support but the ONLY corpus scenario carrying EdgeKind.link, so `link` and its adapter-supplied basis remain UNTESTED ACROSS DIALECTS. That is fidelity evidence in one dialect, not cross-dialect coverage: fixtures/captured/ is not part of the equivalence corpus. This is the most expensive of the three declarations and it stays that way. The previous reason had TWO grounds and one is now RETIRED BY OBSERVATION: a captured GenAI trace containing an invoke_workflow span exists (fixtures/captured/genai_workflow.jsonl, TASKS.md 2.15). The remaining ground is the one that always did the work, and it is untouched: mapping invoke_workflow to chain (SPEC.md 3.2, \"a composite step with no more specific kind\") is a JUDGEMENT rather than the name match every other OPERATIONS entry is. Three things now argue against taking it, and two are MEASURED rather than reasoned. (1) The invoke_workflow span in that capture is HARNESS-EMITTED, and structurally so: an instrumentor wraps SDK calls and a workflow is not one, so no instrumentor can ever emit one. The capture proves the value round-trips and that the adapter degrades honestly on it; it cannot say what a third party means by it, and capture/README.md's own sentence applies -- evidence about the outside world that you generated from your own idea of the outside world is not evidence. (2) Mapping it BREAKS unknown_kind[otel_genai], measured at TASKS.md 2.16: invoke_workflow is the adapter's ONLY unmapped convention value and is that scenario's specimen precisely because it is read from the registry rather than invented, so mapping it leaves otel_genai with no observed unmapped value at all. (3) SPEC.md 3.2 designates unknown + reported_kind as the first-class outcome for exactly this class, naming guardrail / reranker / router / handoff -- all composite-ish kinds a dialect invents. So this declaration is no longer \"pending a capture\"; it is pending a semantic decision about a closed vocabulary, and TASKS.md 2.16 carries it to a human with the rendering already written and verified. WHAT IS MEASURED AND READY (TASKS.md 2.16): with invoke_workflow mapped to chain, the otel_genai rendering recorded there reproduces this expected/graph.json EXACTLY -- `name` compared, no erase declaration, no comparison.json, and no edit to any expected graph. This scenario is one decision away and nothing else.",
    "renderable": false
  }
}
